name: stream_chat_flutter_core_example
description: Example app for testing stream_chat_flutter_core
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  cupertino_icons: ^1.0.3
  flutter:
    sdk: flutter
  stream_chat_flutter_core:
    git:
      url: https://github.com/TheAppCrafter/stream-chat-flutter.git
      path: packages/stream_chat_flutter_core

flutter:
  uses-material-design: true
